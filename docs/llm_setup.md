
This document should explain:

- which environment variables are required
- how to set them on macOS/Linux
- how to set them on Windows
- an example configuration
- how to run the parser after configuration
- security guidance (do not commit API keys)

Example sections:

- Overview
- Required environment variables
- Example setup (macOS/Linux)
- Example setup (Windows PowerShell)
- Running the parser
- Troubleshooting

